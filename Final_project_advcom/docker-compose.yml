version: '3.8'

services:
  db:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: finalcom
      POSTGRES_PASSWORD: finalcom
      POSTGRES_DB: final_advcompro
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
